import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.edtech',  // Change this to your app's unique ID
  appName: 'ComprehensiveEdTech',   // Change this to your app's name
  webDir: 'dist'                    // Ensure this points to Vite’s output folder
};

export default config;
